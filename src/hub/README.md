# SNS-AI Convenience Hub adapter

This directory contains the opt-in, fail-closed adapter that stages canonical convenience-discovery products into `SNS-HUB`, waits for the exact deployed content version, and reconciles social backlinks after durable provider publishing.

It is dormant unless a publish payload explicitly requires the `convenience-discovery-v1` integration or carries a canonical `hubProduct`.
