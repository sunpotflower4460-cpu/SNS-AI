{
  "name": "sns-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Multi-account social publishing bridge for X and Instagram via GitHub Actions.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/publish.mjs && node --check src/providers/x.mjs && node --check src/providers/instagram.mjs"
  }
}
