{
  "name": "sns-ai",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Multi-account autonomous social publishing engine for X and Instagram via GitHub Actions.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/publish.mjs && node --check src/orchestrate.mjs && node --check src/validate-config.mjs && node --check src/providers/x.mjs && node --check src/providers/instagram.mjs",
    "validate": "node src/validate-config.mjs",
    "autopilot": "node src/orchestrate.mjs"
  }
}
